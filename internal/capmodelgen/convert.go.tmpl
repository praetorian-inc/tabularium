// Code generated by capmodelgen; DO NOT EDIT.

package {{.Package}}

import (
{{- if needsJSON .TypeSpecs}}
	"encoding/json"
	"fmt"
{{end}}
	"github.com/praetorian-inc/tabularium/pkg/model/model"
	"github.com/praetorian-inc/tabularium/pkg/registry"
)

func init() {
{{- range .TypeSpecs}}
	registry.Registry.MustRegisterConverter("{{.Name}}", convert{{.Name}})
{{- end}}
}
{{range .TypeSpecs}}
func convert{{.Name}}(data []byte) (registry.Model, error) {
{{- if and (not .Parent) (not (hasRemap .))}}
	var result model.{{.SourceTypeName}}
	if err := registry.UnmarshalModel(data, &result); err != nil {
		return nil, err
	}
	return &result, nil
{{- else}}
	var raw map[string]json.RawMessage
	if err := json.Unmarshal(data, &raw); err != nil {
		return nil, err
	}
{{- range remapFields .}}
	if v, ok := raw["{{.JSONName}}"]; ok {
		delete(raw, "{{.JSONName}}")
{{- range .SourceJSONNames}}
		raw["{{.}}"] = v
{{- end}}
	}
{{- end}}
{{- if .Parent}}
	parentRaw := raw["{{.Parent.JSONName}}"]
	delete(raw, "{{.Parent.JSONName}}")
{{- end}}
	b, err := json.Marshal(raw)
	if err != nil {
		return nil, err
	}
{{- if .Parent}}
	var parentModel registry.Model
	if len(parentRaw) > 0 && string(parentRaw) != "null" {
{{- if .Parent.IsInterface}}
		typeName := "{{.Parent.EmbedType}}"
		var parentObj map[string]json.RawMessage
		if json.Unmarshal(parentRaw, &parentObj) == nil {
			if typeRaw, ok := parentObj["_type"]; ok {
				var t string
				if json.Unmarshal(typeRaw, &t) == nil {
					typeName = t
				}
				delete(parentObj, "_type")
				parentRaw, _ = json.Marshal(parentObj)
			}
		}
		parentModel, err = registry.Registry.Convert(typeName, parentRaw)
{{- else}}
		parentModel, err = registry.Registry.Convert("{{.Parent.EmbedType}}", parentRaw)
{{- end}}
		if err != nil {
			return nil, err
		}
	}
	var result model.{{.SourceTypeName}}
	result.Defaulted()
	if err := json.Unmarshal(b, &result); err != nil {
		return nil, err
	}
{{- if .Parent.Wrap}}
	if parentModel != nil {
		gm, ok := parentModel.(model.GraphModel)
		if !ok {
			return nil, fmt.Errorf("convert{{.Name}}: parent is %T, not model.GraphModel", parentModel)
		}
		result.{{.Parent.SourceFieldName}} = model.NewGraphModelWrapper(gm)
	}
{{- else}}
	if parentModel != nil {
		typed, ok := parentModel.({{.Parent.SourceGoType}})
		if !ok {
			return nil, fmt.Errorf("convert{{.Name}}: parent is %T, not {{.Parent.SourceGoType}}", parentModel)
		}
		result.{{.Parent.SourceFieldName}} = typed
	}
{{- end}}
	if err := registry.CallHooks(&result); err != nil {
		return nil, err
	}
{{- else}}
	var result model.{{.SourceTypeName}}
	if err := registry.UnmarshalModel(b, &result); err != nil {
		return nil, err
	}
{{- end}}
	return &result, nil
{{- end}}
}
{{end}}
